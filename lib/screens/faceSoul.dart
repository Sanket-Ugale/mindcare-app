import 'package:flutter/material.dart';

// ignore: camel_case_types
class faceSoul extends StatelessWidget {
  const faceSoul({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("FaceSoul", style: TextStyle(letterSpacing: 5),),centerTitle: true,),
    );
  }
}