import 'package:flutter/material.dart';

class restpassPage extends StatelessWidget {
  const restpassPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("RESET PASSWORD", style: TextStyle(letterSpacing: 5),),centerTitle: true,),
    );
  }
}